import Foundation

enum Constants {
    enum API {
        static let weatherBaseURL = "https://api.open-meteo.com"
        static let geocodingBaseURL = "https://geocoding-api.open-meteo.com"
    }
}
